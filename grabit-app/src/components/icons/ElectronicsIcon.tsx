import React from 'react';
import { LaptopIcon } from './LaptopIcon';
import { IconProps } from './types';

export const ElectronicsIcon: React.FC<IconProps> = (props) => {
  return <LaptopIcon {...props} />;
};

export default ElectronicsIcon;
